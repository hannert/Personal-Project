using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// Script to control the position of reference points that are set distance away from the camera 
public class CameraRelativeSubPoints : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void changePosition(float x, float y, float z)
    {

    }
}
